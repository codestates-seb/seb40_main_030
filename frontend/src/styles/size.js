const SIZE = {
  CIRCLE_SIZE: `clamp(1.5rem, 5vw, 3rem)`,
  SPACING: `clamp(0.25rem, 2vw, 0.5rem)`,
};

const Z_INDEX = {
  DEFAULT: 1,
  PAGE_WRAPPER: 13,
  FRONT: 1100,
  MIDDLE: 10,
  SPLASH_SCREEN: 99999,
  DESKTOP_WRAPPER: -100,
};

export { Z_INDEX, SIZE };
